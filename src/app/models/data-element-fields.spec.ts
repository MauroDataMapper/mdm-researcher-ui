import { DataElementFields } from './data-element-fields';

describe('DataElementFields', () => {
  it('should create an instance', () => {
    expect(new DataElementFields()).toBeTruthy();
  });
});
